<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestionnaire de Tournois E-Sport</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: linear-gradient(45deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
      background-attachment: fixed;
      font-family: 'Press Start 2P', cursive;
      color: #00ff41;
      overflow-x: hidden;
      position: relative;
    }

    /* Scanlines effect */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: repeating-linear-gradient(
        0deg,
        rgba(0, 255, 65, 0.03),
        rgba(0, 255, 65, 0.03) 1px,
        transparent 1px,
        transparent 2px
      );
      pointer-events: none;
      z-index: 1;
    }

    .arcade-container {
      position: relative;
      z-index: 2;
    }

    .glow-text {
      text-shadow: 0 0 10px #00ff41, 0 0 20px #00ff41, 0 0 30px #ff006e;
      animation: glow-pulse 2s ease-in-out infinite;
    }

    @keyframes glow-pulse {
      0%, 100% {
        text-shadow: 0 0 10px #00ff41, 0 0 20px #00ff41, 0 0 30px #ff006e;
      }
      50% {
        text-shadow: 0 0 20px #00ff41, 0 0 30px #00ff41, 0 0 40px #00d9ff;
      }
    }

    .arcade-btn {
      background: linear-gradient(45deg, #ff006e, #8338ec);
      border: 3px solid #ffbe0b;
      color: white;
      padding: 12px 24px;
      font-size: 14px;
      font-family: 'Press Start 2P', cursive;
      cursor: pointer;
      transition: all 0.2s;
      box-shadow: 0 5px 0 #ff006e, inset 0 -2px 0 rgba(0,0,0,0.2);
      text-transform: uppercase;
    }

    .arcade-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 7px 0 #ff006e, inset 0 -2px 0 rgba(0,0,0,0.2);
    }

    .arcade-btn:active {
      transform: translateY(3px);
      box-shadow: 0 2px 0 #ff006e, inset 0 -2px 0 rgba(0,0,0,0.2);
    }

    .arcade-box {
      background: rgba(0, 0, 0, 0.6);
      border: 4px solid #ffbe0b;
      box-shadow: inset 0 0 20px rgba(255, 190, 11, 0.3), 0 0 30px rgba(255, 0, 110, 0.5);
      padding: 20px;
    }

    .arcade-title {
      font-size: 48px;
      letter-spacing: 3px;
      text-align: center;
    }

    .pixel-divider {
      height: 4px;
      background: linear-gradient(90deg, #ff006e, #ffbe0b, #00ff41, #ffbe0b, #ff006e);
      margin: 20px 0;
    }

    .score-board {
      background: rgba(0, 0, 0, 0.8);
      border: 3px solid #00ff41;
      padding: 15px;
      margin: 20px 0;
      text-align: center;
      color: #00ff41;
      text-shadow: 0 0 5px #00ff41;
    }

    .feature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin: 40px 0;
    }

    .feature-card {
      background: rgba(255, 0, 110, 0.1);
      border: 3px solid #00d9ff;
      padding: 20px;
      text-align: center;
      box-shadow: 0 0 15px rgba(0, 217, 255, 0.5);
      transition: all 0.3s;
    }

    .feature-card:hover {
      border-color: #ffbe0b;
      box-shadow: 0 0 25px rgba(255, 190, 11, 0.8);
      transform: scale(1.05);
    }

    .feature-card h3 {
      font-size: 18px;
      color: #00ff41;
      margin-bottom: 10px;
    }

    .feature-card p {
      font-size: 12px;
      color: #00d9ff;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
    }

    .float-anim {
      animation: float 3s ease-in-out infinite;
    }
  </style>
</head>
<body>
  <div class="arcade-container">
    <!-- Header -->
    <div class="min-h-screen flex flex-col items-center justify-center p-4">
      <div class="arcade-box w-full max-w-4xl float-anim">
        <div class="arcade-title glow-text">
          TOURNOIS E-SPORT
        </div>
        <div class="arcade-title" style="font-size: 32px; color: #00d9ff; margin-top: 10px;">
          GESTIONNAIRE
        </div>
        
        <div class="pixel-divider"></div>

        <div class="score-board">
          <p style="font-size: 20px; margin-bottom: 10px;">Bienvenue</p>
          <p style="font-size: 14px;">Accueil — Gestion de tournois e‑sport</p>
        </div>

        <!-- Features -->
        <div class="feature-grid">
          <div class="feature-card">
            <h3>AUTHENTIFICATION</h3>
            <p>Tokens JWT<br>Inscription & Connexion</p>
          </div>
          <div class="feature-card">
            <h3>TOURNOIS</h3>
            <p>Opérations Complètes<br>Gestion Statuts</p>
          </div>
          <div class="feature-card">
            <h3>ÉQUIPES</h3>
            <p>Management<br>Gestion Capitaines</p>
          </div>
          <div class="feature-card">
            <h3>INSCRIPTIONS</h3>
            <p>Mode Solo & Team<br>Validations Métier</p>
          </div>
        </div>

        <div class="pixel-divider"></div>

        <!-- CTA Buttons -->
        <div style="display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; margin-top: 40px;">
          <a href="/api-docs" class="arcade-btn">
            Documentation
          </a>
          <a href="https://github.com" class="arcade-btn" style="background: linear-gradient(45deg, #00d9ff, #ffbe0b);">
            Repository
          </a>
        </div>

        <div class="score-board" style="margin-top: 40px; font-size: 14px;">
          <p>Testez l'API via Swagger →</p>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div style="text-align: center; padding: 40px 20px; color: #00d9ff; font-size: 14px;">
      <p style="margin-bottom: 10px;">© 2026 Gestionnaire de Tournois E-Sport</p>
      <p style="font-size: 12px; color: #00ff41;">Que la meilleure équipe gagne!</p>
    </div>
  </div>
</body>
</html>
